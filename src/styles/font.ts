import { StyleSheet } from 'react-native';

export const fontConvert = StyleSheet.create({
    sport: {
        textTransform: 'uppercase',
        letterSpacing: 0.3,
        transform: [{scaleY: 1.15}],
    }
});