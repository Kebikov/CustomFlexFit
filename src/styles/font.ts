import { StyleSheet } from 'react-native';


/**
 * @style `Коректировка шрифта для Title.`
 */
export const styleFontConvertForTitle = StyleSheet.create({
    sport: {
        textTransform: 'uppercase',
        letterSpacing: 0.3,
        transform: [{scaleY: 1.15}],
    }
});